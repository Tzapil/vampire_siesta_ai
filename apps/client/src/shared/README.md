# Client Shared Layer

Place low-level UI, generic hooks, and app-agnostic utilities here.

Rules:

- No page orchestration.
- No feature-specific business flows.
- Prefer `@siesta/shared` for pure logic that is also needed on the server.
