version: "3.9"

services:
  mongo:
    image: mongo:6
    restart: unless-stopped
    volumes:
      - mongo-data:/data/db

  app:
    build: .
    restart: unless-stopped
    depends_on:
      - mongo
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: ${PORT:-4000}
      MONGO_URL: ${MONGO_URL:-mongodb://mongo:27017/siesta}
      CLIENT_DIST_PATH: /app/client-dist
    ports:
      - "4000:4000"

volumes:
  mongo-data:

